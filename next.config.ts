import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'images.unsplash.com',
      },
      {
        protocol: 'https',
        hostname: 'darkgreen-hawk-753158.hostingersite.com',
      },
    ],
  },
};

export default nextConfig;
